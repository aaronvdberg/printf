#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stddef.h>
#include <unistd.h>

/**
 * Specifying the prototype will add buffers etc later
 */
int _printf(const char *format, ...);

#endif

